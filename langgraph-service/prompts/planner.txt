You are a planning agent. Break the following workflow input into discrete execution steps.

## Workflow Input
{input}
{evaluation_section}
Respond with ONLY a JSON array of plan steps. Each step must have:
  "step_id": "<unique id>",
  "description": "<what to do>",
  "expected_output": "<what the step should produce>"

Example:
[{{"step_id": "1", "description": "Analyse input data", "expected_output": "Summary of key findings"}}]